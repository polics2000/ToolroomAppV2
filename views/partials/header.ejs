<!DOCTYPE html>
<html>
	<head>
		<!-- Make site responsive on mobile/tablet -->
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>Toolroom</title>
		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
		<link rel="stylesheet" href="/stylesheets/main.css">
	</head>
	<body>
		<nav class="navbar navbar-default">
			<div class="container">
				<div class="navbar-header">
					<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
					  <span class="sr-only">Toggle navigation</span>
					  <span class="icon-bar"></span>
					  <span class="icon-bar"></span>
					  <span class="icon-bar"></span>
					</button>
					<a class="navbar-brand" href="/">Toolroom</a>
				</div>
				<div id="navbar" class="collapse navbar-collapse">
				  <ul class="nav navbar-nav">
					<li class="<%= typeof page !== 'undefined' && page === 'campgrounds' ? 'active' : '' %>"><a href="/campgrounds">Home</a></li>
					<!-- <li class="<%= typeof page !== 'undefined' && page === 'about' ? 'active' : '' %>"><a href="/about">About</a></li> -->
					<li class="<%= typeof page !== 'undefined' && page === 'status' ? 'active' : '' %>"><a href="/status">Tool Status</a></li>
					  
					<li class="<%= typeof page !== 'undefined' && page === 'supplier' ? 'active' : '' %>"><a href="/supplier">Supplier</a></li>
					<li class="<%= typeof page !== 'undefined' && page === 'budget' ? 'active' : '' %>"><a href="/budgetcontrol">BudgetControl</a></li>
					<li class="<%= typeof page !== 'undefined' && page === 'todo' ? 'active' : '' %>"><a href="/todo">TodoList</a></li>
					<li class="<%= typeof page !== 'undefined' && page === 'colorgame' ? 'active' : '' %>"><a href="/colorgame">RGB ColorGuess</a></li>
					<li class="<%= typeof page !== 'undefined' && page === 'colorgame' ? 'active' : '' %>"><a href="/piggame">Piggame</a></li>

				  </ul>
					
				  <ul class="nav navbar-nav navbar-right">
					  <% if(!currentUser){ %>
						  <li class="<%= typeof page !== 'undefined' && page === 'login' ? 'active' : '' %>"><a href="/login">Login</a></li>
						  <li class="<%= typeof page !== 'undefined' && page === 'register' ? 'active' : '' %>"><a href="/register">Sign Up</a></li>
					  <% } else { %>
						  <li><a href="#">Signed In As <%= currentUser.username %></a></li>
						  <li><a href="/logout">Logout</a></li>
					  <% } %>
				  </ul>
				</div>
			</div>
		</nav>
		
		<div class="container">
			<% if(error && error.length > 0) { %>
				<div class="alert alert-danger" role="alert">
					<h1 class="font-size"><%= error %></h1>
				</div>
			<% } %>
			<% if(error && success.length > 0) { %>
				<div class="alert alert-success" role="alert">
					<h1 class="font-size"><%= success %></h1>
				</div>
			<% } %>
		</div>
		<div class="container">
			